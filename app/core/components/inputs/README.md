# inputs/

> **Це дорожня карта, а не опис наявного.** Реалізовано в цій папці: `UITextField`, `UITextarea`, `UISelect`, `UICheckbox`, `UILabel`.
> Решта рядків таблиці — заплановане; оцінки годин лишилися з вихідного проєкту.

| `UITextField`
| `UITextarea`
| `UISelect` 
| `UIAutocomplete`
| `UIMultiSelect` 
| `UIRadioGroup` + `UIRadio`
| `UICheckbox` 
| `UICheckboxGroup` 
| `UISwitch` 
| `UIFileInput` 
| `UINumberInput` 
